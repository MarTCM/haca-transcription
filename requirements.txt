# Core ASR engine. Pulls in ctranslate2 (inference backend), tokenizers,
# av/PyAV (media decoding, incl. video), onnxruntime + the bundled Silero VAD.
faster-whisper>=1.1.0

# Optional GPU probe used by --device auto. faster-whisper does not require
# torch, but if it is present we use it to detect a CUDA device.
# torch  # install the build matching your CUDA (see README)

# Tests
pytest>=8.0
